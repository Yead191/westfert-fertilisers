import AppSupport from "@/components/web-pages/settings/app-support";
import React from "react";

const page = () => {
  return (
    <section>
      <AppSupport />
    </section>
  );
};

export default page;
