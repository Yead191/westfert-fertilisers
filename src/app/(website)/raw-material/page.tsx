import RawMaterial from "@/components/web-pages/raw-material";
import React from "react";

const page = () => {
  return (
    <section>
      <RawMaterial />
    </section>
  );
};

export default page;
