import QuoteUpdate from "@/components/web-pages/quote-update";
import React from "react";

const page = () => {
  return (
    <section>
      <QuoteUpdate />
    </section>
  );
};

export default page;
