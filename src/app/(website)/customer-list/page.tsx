import CustomerList from "@/components/web-pages/customer-list";
import React from "react";

const page = () => {
  return (
    <section>
      <CustomerList />
    </section>
  );
};

export default page;
