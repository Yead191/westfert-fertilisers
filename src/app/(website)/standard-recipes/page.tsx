import StandardRecipes from "@/components/web-pages/standard-recipes";
import React from "react";

const page = () => {
  return (
    <section>
      <StandardRecipes />
    </section>
  );
};

export default page;
